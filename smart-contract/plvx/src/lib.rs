#![cfg_attr(not(test), no_std)]
#![cfg_attr(not(test), no_main)]
extern crate alloc;

// Original implementation (reference)
pub mod premier_league;

// Improved implementation with security fixes
pub mod premier_league_improved;
